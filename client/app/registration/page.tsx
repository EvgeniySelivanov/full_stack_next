import * as React from 'react';
import RegistrationForm  from '../components/Forms/Registration/Form';

interface IAppProps {
}

const Form: React.FunctionComponent<IAppProps> = (props) => {
  return  <RegistrationForm />;
};

export default Form;

